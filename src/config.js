export default {
    api: {
        host: "https://jsonplaceholder.typicode.com",
        paths: {
            users: "/users",
            posts: "/posts",
            comments: "/comments"
        }
    } 
};
